import { style } from '@vanilla-extract/css';

export const SpreadSheet = style({
  width: '100%',
  height: '100%',
  overflow: 'hidden',
  display: 'flex',
  flexDirection: 'column',
});
export const SpreadSheetContainer = style({
  width: '100%',
  height: '100%',
  overflow: 'scroll',
});
